{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "repo_root=$(git rev-parse --show-toplevel 2>/dev/null || pwd); bash \"$repo_root/.codex/hooks/cleanup-tmp.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
